**<span style="color:#56adda">0.0.5</span>** *(marcosviniciusi)*
- Fix: Skip invalid `-c:t:N` and `-c:d:N` FFmpeg specifiers for attachment and data streams
- Prevents FFmpeg errors when processing files with embedded fonts (e.g., anime MKV with TTF attachments)

**<span style="color:#56adda">0.0.4</span>** *(marcosviniciusi)*
- Renamed plugin from `ignore_video_file_over_resolution` to `ignore_video_over_res` to avoid conflict with official repo

**<span style="color:#56adda">0.0.3</span>** *(marcosviniciusi)*
- Standardized plugin name to "Ignore - Video Over Resolution Limit" for consistent naming
- Updated author attribution for custom repository

**<span style="color:#56adda">0.0.2</span>**
- Added debug logging when the file is not to be ignored (Josh.5)

**<span style="color:#56adda">0.0.1</span>**
- Initial version (Josh.5)
