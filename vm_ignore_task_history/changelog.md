**<span style="color:#56adda">0.0.3</span>** *(marcosviniciusi)*
- Renamed plugin from `ignore_completed_tasks` to `ignore_task_history` to avoid conflict with official repo

**<span style="color:#56adda">0.0.2</span>** *(marcosviniciusi)*
- Standardized plugin name to "Ignore - Completed Tasks" for consistent naming
- Updated author attribution for custom repository

**<span style="color:#56adda">0.0.1</span>**
- Initial version (Josh.5)
